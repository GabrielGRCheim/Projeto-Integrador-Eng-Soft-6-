import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "#FFF",
        width: "48%",
        borderRadius: 12,
        padding: 16,
        elevation: 3,
        marginBottom: 10,
    },

    value: {
        fontSize: 24,
        fontWeight: "bold",
    },

    title: {
        color: "#666",
        marginTop: 5,
    },
});